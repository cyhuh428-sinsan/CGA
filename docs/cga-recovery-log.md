# CGA 복구 작업현황

## 2026-07-17 — 복구 기준 및 현재 장애 기록

- 복구 기준 사용자: `cyhuh` (허철영, `group_admin`). `admin`을 기준 사용자로 사용하지 않는다.
- 복구 기준 선택 상태: `g-support` / Support Bot Group / Aidot 봇 (`175c6113-ac68-4ca8-9e1b-70391581944e`) / 작업 버전 `v1`.
- DB 확인 결과: Support Bot Group에는 CGA Bot, 테스트 봇 20260701, Aidot 봇이 존재한다. Ops Assistant만 보인 현상은 `g-ops`가 선택된 상태였으며, 봇 삭제 증거는 확인되지 않았다.
- 확인된 화면 장애: `#entity-management`, `#dictionary-management` 본문 섹션은 HTML에 있으나, 이를 채우는 `apps/studio/asset-management.js`가 `apps/studio/app.js`에서 import·초기화되지 않아 빈 화면으로 표시됐다.
- 1차 복구 조치: 자산 관리 모듈을 앱 렌더링 순서에 다시 연결하고, 개체·사전 화면에서는 공통 저장 버튼을 숨김 처리했다. 이 조치는 DB 저장 로직이나 봇/그룹 데이터에 쓰지 않는다.
- 검증: `node --check apps/studio/app.js` 통과. 브라우저 자동 클릭 채널은 사용할 수 없어, 실제 화면 클릭 검증은 보류다.
- 재발 방지 기록 항목: 이후 각 작업에서 사용자·그룹·봇·버전·서버 PID·캐시 버전·수정 파일·검증 결과·실패 원인·복구 기준을 이 파일에 추가한다.

- 화면 캐시 버전: `20260717-25`. 로컬 서버는 최신 `app.js`, `asset-management.js`, `styles.css`를 HTTP 200으로 제공함.

- 실제 브라우저 클릭 검증: 자동 브라우저 연결이 시간 초과로 초기화되어 이번 단계에서는 수행하지 못함. HTTP 200 제공·JS 구문·cyhuh API 데이터 조회로 대체 확인했으며, 다음 단계 시작 전 실제 클릭 확인을 다시 수행한다.

## 2026-07-17 — 목록 화면 표준 정리

- 기준 해상도: 1920×1080, 기본 본문/목록 글자 12px.
- 사전·개체 목록: 화면 잔여 높이가 제어줄 사이 여백으로 분배되지 않도록 목록 영역을 `헤더 / 제어줄 / 데이터 스크롤 / 페이지`의 고정 순서로 배치했다. 데이터가 넘칠 때에는 데이터 본문만 스크롤한다.
- 개체값 조회: 체크박스에 일반 입력창의 높이·테두리 규칙이 적용되던 문제를 분리해 14px 체크박스로 복구했다.
- 의도/모듈 목록: 내부 UUID는 클릭 식별자로만 유지하고, 화면에는 의도/모듈명과 표시명을 출력한다. 9개 실제 열에 맞춰 격자 폭을 재정의하고 긴 값은 한 줄 말줄임으로 표시한다.
- 화면 캐시 버전: `20260717-26`.
- 검증: `node --check apps/studio/app.js` 통과, `git diff --check` 통과, 로컬 서버에서 `styles.css?v=20260717-26` 및 `app.js?v=20260717-26` HTTP 200 확인.
- 제한: 브라우저 자동 클릭 연결은 이전과 동일하게 시간 초과 상태여서, 이번 단계의 실제 클릭 검증은 사용자 브라우저 확인 후 이어서 기록한다.
## 2026-07-17 봇 제작 목록 표준 보완
- 상태: 로컬 구현 완료, 브라우저 실제 클릭 검증 보류.
- 변경: 본문 설명을 Help 툴팁으로 이동하고, 검색/필터·추가·점3개·선택/삭제·행별 점3개·하단 페이지 표시를 봇 제작 목록에 적용.
- 안전: 기존 데이터 및 DB를 변경하지 않았으며, 관련 없는 작업 트리 변경은 포함하지 않음.
- 검증: node --check apps/studio/app.js 통과.

## 2026-07-17 복구 중 파일 쓰기 장애
- 원인: 긴 전체 파일 쓰기 명령을 중단하면서 app.js가 0바이트가 됨.
- 조치: 로컬 Git 객체의 정상 커밋 1a4cc7c에서 app.js, styles.css, index.html을 원자적으로 복원.
- 검증: app.js 713,386 bytes, node --check 통과, 로컬 서버 제공 확인.
- 재발 방지: 전체 파일 직접 쓰기 금지, 임시 파일 검증 후 원자적 교체만 사용.


## 2026-07-17 복구 재개 — 문서 이력 대조
- 판정: 중대 미진 사항. 현재 대화 설계는 사용자 흐름에서 표시되지 않아 미복구로 재판정.
- 확인: 앱 코드에는 design 렌더러가 남았지만, 실제 대화 시작 화면에서 Aidot형 캔버스·속성 패널·도구 팔레트가 나타나지 않음.
- 공통 누락: 전체 건수·보기·삭제/선택 건수, 행별 ⋮, 데이터 영역 스크롤, 작업영역 최하단 페이지 표시.
- 조치 순서: 아이콘/하위메뉴 → 개체·사전 목록 공통 표준 → 의도·모듈 목록 표준 → 대화 시작/대화 설계 → 봇 테스트·평가.
- 검증 상태: 정적 코드 확인만 수행. 브라우저 자동 검증 연결은 시간 초과로 미검증.


## 2026-07-17 대화 설계 진입 복구 1단계
- 원인: `저장 후 대화설계`가 서버 상세자산 저장 완료를 먼저 기다려 저장 큐가 지연될 때 화면 전환이 발생하지 않았다.
- 변경: 해당 버튼은 캔버스로 즉시 전환하고, 동일한 저장 작업은 비동기로 실행하도록 한정 수정했다. 대화 카드 데이터·라우팅·DB 스키마는 변경하지 않았다.
- 검증: `node --check apps/studio/app.js` 통과. 로컬 4173 제공본에서 변경된 전환 처리 문자열을 확인했다.
- 미검증: 브라우저 자동 연결 시간 초과로 실제 클릭·캔버스 표시 확인은 아직 미완료다.

## 2026-07-17 목록 선택 표준 복구 1단계
- 범위: 사전 관리와 개체 관리가 공유하는 `apps/studio/asset-management.js` 목록 제어부.
- 변경: 미선택 상태는 `전체 N건 · N개씩 보기 · 삭제`, 선택 상태는 `전체 N건 · N개씩 보기 · N개 선택`으로 렌더링하도록 적용했다.
- 유지: 행/전체 선택, 페이지 크기, 사전 의도 사용·미사용 기능, 파일 메뉴는 변경하지 않았다.
- 검증: `node --check apps/studio/asset-management.js` 통과, 선택 상태 렌더링 문자열 확인.
- 미검증: 브라우저 자동 연결 시간 초과로 실제 클릭 및 화면 확인은 미완료다.

## 2026-07-17 대화 시작 화면 복구 1단계
- 원본 대조: `D:/Project/cga/Aidot/apps/web/components/intent-start-page.tsx`의 좌측 학습문장·우측 추출 개체 2열 구조를 확인했다.
- 원인: CGA의 `.aidot-dialog-start`는 2열 CSS였지만 좌측 section만 렌더링해서 우측이 비어 있었다.
- 변경: `apps/studio/app.js`에 우측 추출 개체 패널, 선택 개체 추가/삭제 제어, 선택 목록과 빈 상태를 추가했다. 기존 개체 선택 모달을 연결해 별도 임시 데이터 구조는 만들지 않았다.
- 스타일: `apps/studio/styles.css`에 해당 패널의 표·빈 상태 레이아웃만 추가했다.
- 검증: `node --check apps/studio/app.js` 통과.
- 미검증: 브라우저 자동 연결이 시간 초과여서 실제 클릭·레이아웃 검증은 미완료다.

## 2026-07-17 Git 기록 상태
- `git status --short -- apps/studio/asset-management.js apps/studio/styles.css docs/cga-recovery-log.md`를 실행했으나 19초 후 시간 초과했다.
- 따라서 이번 복구 단계는 아직 commit/push하지 않았다. 더티 작업트리에 다른 변경이 다수 있어 상태 확인 없이 강제 stage 또는 push하지 않았다.
- 소스와 로컬 제공본 검증은 완료했으며, Git 응답 정상화 후 이 단계 파일만 분리해 commit/push해야 한다.


## 2026-07-17 목록 화면 표준화 단계

- 범위: 제작 > 의도/모듈 목록, 제작 > 개체 관리, 제작 > 사전 관리.
- 변경:
  - 개체/사전 목록의 페이지 영역을 작업영역 최하단으로 보내고, 목록 본문만 스크롤하도록 높이 구조를 수정했다.
  - 사전의 `의도 사용`/`의도 미사용`은 선택된 항목이 있을 때만 우측 `단어 추가` 아래에 표시하도록 이동했다.
  - 의도/모듈 목록에 검색·유형 필터, 선택 수, 페이지 크기, 정렬 헤더, 행별 점 메뉴, 우측 추가/점 메뉴를 추가했다.
  - 제작 목록의 상시 안내 문구와 외곽 저장 버튼을 제거하고, 운영 버전 제한 문구는 Help 아이콘의 툴팁으로 전환했다.
- 검증:
  - `node --check apps/studio/app.js` 통과.
  - `node --check apps/studio/asset-management.js` 통과.
  - 로컬 서버 재시작 후 `served-list-standard=ok`로 최신 app.js/styles.css 제공을 확인했다.
- 미완료:
  - 브라우저 자동 클릭 채널은 사용할 수 없어 실제 화면 클릭·레이아웃 검증은 아직 수행하지 못했다.
  - Git 상태 확인/커밋/푸시는 이전 명령 지연과 원격 DNS 문제를 재확인한 뒤, 이 단계의 파일만 분리해 진행한다.

- Git 기록 시도: 대상 4개 파일만 `git add` 했으나 34초 후 시간 초과했다. 스테이징/커밋/푸시 성공 여부를 확인할 수 없으므로 성공으로 기록하지 않는다.

## 2026-07-17 제작 목록 공통 표준 보완 1단계
- 근거: `CGA 작업 History.docx`의 Aidot 목록 표준과 최근 화면 캡처를 대조했다.
- 변경: 제작 목록을 기본 50개 보기로 설정하고, 선택 시 `삭제` 뒤 `N개 선택`을 표시했다. 중복 CSS 그리드 정의를 11열(선택/ID/구분/명칭/표시명/수치/수정정보/행 메뉴)로 통일해 열 어긋남을 제거했다.
- 변경: 제작 제목 설명과 운영버전 안내는 고정 본문 대신 제목 옆 Help 아이콘 툴팁으로 바꿨으며, 목록 영역이 남은 작업 높이를 쓰고 표 본문만 스크롤하도록 조정했다.
- 보존: 로그인·권한·봇/버전 선택·서버/DB 데이터는 수정하지 않았다.
- 검증 예정: JS 구문 검사, 로컬 정적 제공 확인 후 브라우저 실제 클릭은 별도 확인한다.

- 검증 완료: `node --check apps/studio/app.js`, `node --check apps/studio/asset-management.js` 통과. 로컬 `127.0.0.1:4173`가 수정된 app.js/styles.css를 제공하는 것도 확인했다.
- 미완료: 실제 브라우저 클릭·1920×1080 시각 검증은 자동 브라우저 채널 없이 수행하지 않았다.

- 후속 보완: 제작 화면의 제목 글꼴을 16px 표준으로 통일하고, 목록은 남은 작업영역을 사용하며 표 본문만 스크롤되도록 변경했다. 페이지 영역은 목록의 마지막 행에 고정된다.
- 검증: `node --check apps/studio/app.js`, `node --check apps/studio/asset-management.js` 통과 및 로컬 정적 제공 확인.
- Git 상태: 기존 멈춤 Git 프로세스가 인덱스를 점유해 기록 작업을 방해했다. 잠금 프로세스를 종료한 뒤 이 단계 기록을 재시도한다.

## 2026-07-17 개체·사전 Help 표준 보완
- 개체 관리·사전 관리의 고정 설명을 제거하고 제목 옆 Help 툴팁으로 전환했다.
- 목록 데이터, 선택, 추가/파일 메뉴, 저장 API, 로그인·권한은 변경하지 않았다.
- 검증: `node --check apps/studio/asset-management.js` 통과 및 로컬 정적 제공 확인.
- Git: 활성 Git 프로세스가 다시 인덱스를 점유하면 이 단계는 즉시 기록하지 않고 원인으로 남긴다.


## 2026-07-17 목록 선택 제어행 복구

- 사전/개체 목록에서 행을 선택하면 삭제 버튼이 사라지던 문제를 수정했다.
- 표준 제어행을 항상 `전체 N건 · 페이지 크기 · 삭제 · N개 선택` 순서로 유지한다.
- 삭제 버튼은 선택 전에는 비활성, 선택 후에는 활성으로 표시한다.
- 사전의 `의도 사용`/`의도 미사용`은 선택 시에만 `단어 추가` 아래 오른쪽 보조행에 표시한다.
- 제작 목록 검색 필드는 돋보기·입력창·구분 필터를 하나의 좌측 검색 행으로 묶어 제어 요소가 분산되지 않도록 보완했다.

검증:
- `node --check apps/studio/app.js` 통과.
- `node --check apps/studio/asset-management.js` 통과.
- 로컬 `127.0.0.1:4173`에서 최신 `app.js`, `asset-management.js` 정적 제공 확인.
- 브라우저 실제 클릭 검증은 이 세션에서 자동 브라우저 채널을 사용할 수 없어 보류.

## 2026-07-17 — 사전/개체값 편집 모달 제어 정렬
- 단어 수정/등록 모달의 의도 사용여부를 체크박스와 문구가 분리되지 않는 단일 인라인 제어로 수정했다.
- 개체값 생성/수정 모달의 유형 라디오 라벨을 인라인 정렬로 통일했다.
- 데이터 저장·삭제·개체값 등록 흐름은 변경하지 않았다.
- 검증: 
ode --check apps/studio/asset-management.js 통과.
- 미검증: 브라우저 실제 클릭 시각 검증은 이 단계에서 실행하지 않음.

## 2026-07-17 — 사전/개체값 편집 모달 보완 및 파일 복원
- 단어 수정/등록 모달의 의도 사용여부를 체크박스와 문구가 한 제어 안에 표시되도록 수정했다.
- 개체값 생성/수정에서 유형 라디오를 인라인 정렬하고, 패턴/동의어 입력 글자 수·목록 헤더·전체 선택을 추가했다.
- 작업 중 pps/studio/asset-management.js가 0바이트가 된 것을 발견했고, Git 인덱스에 있던 동일 파일의 작업본(31,031바이트)만 복원한 후 위 변경을 재적용했다. DB·인증·다른 소스 파일은 복원하거나 변경하지 않았다.
- 검증: 
ode --check apps/studio/asset-management.js 통과.
- 미검증: 브라우저 실제 모달 클릭·저장 검증은 실행하지 않음.

## 2026-07-17 — 목록 제어행 정렬
- 사전 목록에서 의도 사용/의도 미사용을 별도 줄에서 제거했다.
- 같은 목록 제어행의 오른쪽 끝으로 이동했고, 왼쪽에는 전체 건수·페이지 크기·삭제·선택 수를 유지했다.
- 선택하지 않아도 삭제 버튼이 표시되며, 선택 시에만 활성화된다.
- 검증: 
ode --check apps/studio/asset-management.js 통과.


## 2026-07-17 — 공통 목록 선택 제어 보완

- 개체·사전 공통 목록에서 선택 상태와 무관하게 `삭제` 제어가 DOM 및 레이아웃에서 항상 유지되도록 전용 클래스를 부여했다.
- 선택된 건수는 삭제 버튼 뒤에 표시하고, 사전의 `의도 사용`·`의도 미사용`만 동일 제어행의 오른쪽 끝에 배치했다.
- 변경 범위는 `apps/studio/asset-management.js`, `apps/studio/styles.css`로 한정했으며 DB·인증·라우팅은 변경하지 않았다.
- 확인: `node --check apps/studio/asset-management.js` 통과.
- 실제 브라우저 자동 검증은 브라우저 연결 시간 초과로 보류되어 수동 화면 확인이 남아 있다.


## 2026-07-17 — 봇 제작 의도/모듈 생성·수정 진입 보완
- 변경: `의도/모듈 추가`는 Aidot 기준의 생성 모달(구분, 의도명, 표시명, 생성 Key, 전환잠금·복귀차단·피드백 옵션, 태그, 취소/확인)을 표시하도록 보완했다.
- 변경: 생성 저장 시 입력한 `표시명`과 `구분(의도/모듈)`을 실제 자산 상태에 반영하도록 수정했다. 기존에는 생성 함수가 이를 무시해 화면과 저장값이 불일치할 수 있었다.
- 변경: 목록의 각 행 점 3개는 대화 시작으로 이동하지 않고 해당 행의 `의도 수정` 모달을 연다. 수정 시 기존 ID는 보존하고 표시명·구분만 반영한다.
- 범위: `apps/studio/app.js`, `apps/studio/styles.css`만 수정했다. DB, 인증, 라우팅, 캐시 버전, 서버 재시작은 변경하지 않았다.
- 확인: `node --check apps/studio/app.js` 통과. 로컬 서버가 변경 마커(`data-build-intent-return-block`, `data-build-intent-edit`, 모달 CSS)를 제공하는 것을 HTTP로 확인했다.
- 미완료/주의: 실제 브라우저 클릭 검증은 브라우저 연결이 60초 시간 초과로 다시 중단되어 보류다. 이 단계에서는 이를 통과로 보고하지 않는다.
- Git 상태 확인 및 push는 2026-07-17 현재 `git status`가 30초 이상 응답하지 않아 보류했다. 잠금 해제·상태 확인 전에는 commit/push 성공으로 기록하지 않는다.
